import {SiteHeader} from '@/components/SiteHeader'; import {SiteFooter} from '@/components/SiteFooter'; import Link from 'next/link'; import {companies,projects} from '@/lib/data';
export default function Featured(){return <><SiteHeader/><main className="shell"><section className="page-hero"><div className="eyebrow">Editorial selection</div><h1>FEATURED</h1><p>A selected view of companies and projects with profiles built for clearer discovery. Current entries are marked demonstration content.</p></section><section className="section"><div className="section-head"><h2>PROJECTS</h2></div><div className="actions">{projects.map(p=><Link className="btn" key={p.slug} href={`/projects/${p.slug}`}>{p.name}</Link>)}</div></section><section className="section" style={{paddingTop:0}}><div className="section-head"><h2>COMPANIES</h2></div><div className="actions">{companies.map(c=><Link className="btn" key={c.slug} href={`/companies/${c.slug}`}>{c.name}</Link>)}</div></section></main><SiteFooter/></>}
