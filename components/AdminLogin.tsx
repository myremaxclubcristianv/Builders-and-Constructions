'use client';
import {FormEvent,useState} from 'react'; import {createClient} from '@supabase/supabase-js';
export function AdminLogin(){const [error,setError]=useState('');const [loading,setLoading]=useState(false);async function login(event:FormEvent<HTMLFormElement>){event.preventDefault();setLoading(true);setError('');const f=new FormData(event.currentTarget);const url=process.env.NEXT_PUBLIC_SUPABASE_URL;const key=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;if(!url||!key){setError('Admin authentication is not configured.');setLoading(false);return}const client=createClient(url,key);const {data,error:authError}=await client.auth.signInWithPassword({email:String(f.get('email')),password:String(f.get('password'))});if(authError||!data.session){setError(authError?.message||'Unable to sign in.');setLoading(false);return}const response=await fetch('/api/admin/session',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({accessToken:data.session.access_token})});if(!response.ok){const body=await response.json();setError(body.error||'This account is not authorized.');setLoading(false);return}window.location.assign('/admin')}return <form className="form-grid" onSubmit={login}><label><span className="form-label">Email</span><input name="email" type="email" required autoComplete="email"/></label><label><span className="form-label">Password</span><input name="password" type="password" required autoComplete="current-password"/></label>{error&&<p className="form-error full" role="alert">{error}</p>}<button className="btn fill full" disabled={loading}>{loading?'Signing in…':'Admin login'}</button></form>}
