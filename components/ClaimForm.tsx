'use client';
import {FormEvent,useState} from 'react';
export function ClaimForm({companySlug,companyName}:{companySlug:string;companyName:string}){const [state,setState]=useState<'idle'|'sending'|'sent'|'error'>('idle');const [error,setError]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setState('sending');const f=new FormData(e.currentTarget);const r=await fetch('/api/claims',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({companySlug,name:f.get('name'),company:f.get('company'),email:f.get('email'),phone:f.get('phone'),role:f.get('role'),website:f.get('website'),message:f.get('message')})});const j=await r.json();if(!r.ok){setError(j.error);setState('error')}else setState('sent')}if(state==='sent')return <p className="form-status" role="status">Claim received. Our editorial team will verify it before any access is granted.</p>;return <form className="form-grid" onSubmit={submit}><label><span className="form-label">Name</span><input name="name" required placeholder="Name *"/></label><label><span className="form-label">Company</span><input name="company" defaultValue={companyName} required placeholder="Company *"/></label><label><span className="form-label">Business email</span><input name="email" type="email" required placeholder="Business email *"/></label><label><span className="form-label">Phone</span><input name="phone" placeholder="Phone"/></label><label><span className="form-label">Role</span><input name="role" required placeholder="Your role *"/></label><label><span className="form-label">Website</span><input name="website" type="url" placeholder="Website"/></label><label className="full"><span className="form-label">Message</span><textarea name="message" placeholder="How can we verify your relationship to this company?"/></label>{state==='error'&&<p className="form-error full" role="alert">{error}</p>}<button className="btn fill full" disabled={state==='sending'}>{state==='sending'?'Submitting…':'Claim this company'}</button></form>}
